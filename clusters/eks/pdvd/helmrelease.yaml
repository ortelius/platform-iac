# Auto-generated by Terraform
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pdvd
  namespace: flux-system
spec:
  interval: 5m
  chart:
    spec:
      chart: ./chart/pdvd
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  valuesFrom:
    # Sensitive values: baseUrl, rbac_repo_token, github.appId,
    # github.clientId, github.clientSecret, github.privateKey, smtp.*
    - kind: Secret
      name: pdvd-secrets
      valuesKey: values.yaml
  values:
    # Non-sensitive values written here take lower precedence than valuesFrom,
    # but are listed explicitly so the chart's required fields are always visible.
    pdvd-frontend:
      ingress:
        enabled: true
        type: alb
        host: eks.deployhub.com
        certificateArn: arn:aws:acm:us-east-1:754991274490:certificate/bf119c31-1aa2-4270-903a-7b08d1a3a0d8
        subnets: "subnet-0d6b1c5793673a4f8,subnet-09fb2dcb293f6ebcf"

    pdvd-backend:
      ingress:
        enabled: true
        type: alb
        host: eks.deployhub.com
        certificateArn: arn:aws:acm:us-east-1:754991274490:certificate/bf119c31-1aa2-4270-903a-7b08d1a3a0d8
        subnets: "subnet-0d6b1c5793673a4f8,subnet-09fb2dcb293f6ebcf"
      rbac_repo: https://github.com/ortelius/pdvd-rbac
      apiBaseUrl: https://eks.deployhub.com/api
      github:
        appName: pdvd
        org: ortelius
