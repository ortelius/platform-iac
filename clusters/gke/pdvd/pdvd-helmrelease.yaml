apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: pdvd
  namespace: flux-system   
spec:
  interval: 10m
  targetNamespace: pdvd     
  install:
    createNamespace: true
  chart:
    spec:
      chart: pdvd
      version: "12.0.112"
      sourceRef:
        kind: HelmRepository
        name: pdvd-charts
        namespace: flux-system
      interval: 1h
  valuesFrom:
    - kind: ConfigMap
      name: pdvd-values
    - kind: Secret
      name: pdvd-secrets
